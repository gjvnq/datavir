* Stop using the atomic thing for inodes, just use the DB and primary keys
* Replace the "All Bundles" directory with one for each volume. This will help with bundle creation.
* Add soft delete (i.e. trash folder)
